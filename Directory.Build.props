<Project>
    <PropertyGroup>
        <LangVersion>9.0</LangVersion>
    </PropertyGroup>
    <PropertyGroup Condition="'$(MSBuildProjectName)' == 'Assembly-CSharp'">
        <Nullable>enable</Nullable>
    </PropertyGroup>
</Project>